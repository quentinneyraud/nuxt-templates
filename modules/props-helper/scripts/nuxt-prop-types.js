export const notRequiredString = {
  type: String,
  required: false,
  default: null
}

export const notRequiredStringWithDefault = defaultValue => ({
  type: String,
  required: false,
  default: defaultValue
})

export const notRequiredObject = {
  type: Object,
  required: false,
  default: null
}

export const notRequiredObjectWithDefault = defaultValue => ({
  type: Object,
  required: false,
  default: defaultValue
})

export const notRequiredArray = {
  type: Array,
  required: false,
  default: null
}

export const notRequiredArrayWithDefault = defaultValue => ({
  type: Array,
  required: false,
  default: defaultValue
})

export const notRequiredBoolean = {
  type: Boolean,
  required: false,
  default: false
}

export const notRequiredBooleanWithDefault = defaultValue => ({
  type: Boolean,
  required: false,
  default: defaultValue
})

export const notRequiredNumber = {
  type: Number,
  required: false,
  default: null
}

export const notRequiredNumberWithDefault = defaultValue => ({
  type: Number,
  required: false,
  default: defaultValue
})
